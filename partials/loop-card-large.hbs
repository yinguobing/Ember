{{!-- Large Post Card - 参考 Imperia 设计 --}}
<article class="gh-card gh-card-large {{post_class}}">
    <a href="{{url}}" class="gh-card-link"></a>
    
    <div class="gh-card-image-holder">
        {{#if feature_image}}
            <div class="gh-card-image">
                <img src="{{img_url feature_image size="l"}}" 
                     srcset="{{img_url feature_image size="m"}} 600w,
                             {{img_url feature_image size="l"}} 960w,
                             {{img_url feature_image size="xl"}} 1200w"
                     sizes="(max-width: 768px) 100vw, 600px"
                     alt="{{title}}" 
                     loading="lazy">
            </div>
        {{/if}}

        {{!-- 门槛阶梯：公开不标 / 免费会员低调 / 课程会员强标。
             徽章放在 {{#if feature_image}} 之外 —— 无封面图的文章同样有标记。
             绝对定位相对 .gh-card-image-holder（已有 position: relative）。 --}}
        {{#match visibility "paid"}}
        <span class="gh-card-premium-badge" title="课程会员专享">课程会员</span>
        {{else match visibility "members"}}
        <span class="gh-card-member-badge" title="免费会员可读">会员</span>
        {{/match}}
    </div>    
    <div class="gh-card-content">
        {{#primary_tag}}
            <a href="{{url}}" class="gh-card-tag">{{name}}</a>
        {{/primary_tag}}
        <h3 class="gh-card-title"><a href="{{url}}">{{title}}</a></h3>
        {{#if excerpt}}
            <p class="gh-card-excerpt">{{excerpt words="25"}}</p>
        {{/if}}
        <div class="gh-card-meta">
            {{#foreach authors}}{{#if @first}}{{name}}{{/if}}{{/foreach}}
            <span class="gh-meta-sep">&middot;</span>
            <time datetime="{{date format="YYYY-MM-DD"}}">{{date format="YYYY年M月D日"}}</time>
        </div>
    </div>
</article>
